# 素材本地化清单

这份清单用于把当前项目里依赖 Figma 临时链接的素材，替换成项目本地静态资源。

目标：

- 提升本地预览加载速度
- 避免 Figma 临时链接过期
- 为后续静态部署做准备

建议统一放到：

```text
public/assets/
```

建议目录结构：

```text
public/assets/
  home/
  design/
  detail/
  common/
  qr/
```

## 1. 首页 `hello world`

来源文件参考：

- `src/content/figmaAssets.ts`
- `src/content/tabs.ts`

建议落地素材：

1. `public/assets/home/board-texture.webp`
   - 用途：主画布底图
   - 当前远程字段：`boardTexture`
   - 建议：如果这是统一底板，后续其他 tab 也复用这张

2. `public/assets/home/badge-left.png`
   - 用途：左侧工牌挂件
   - 当前远程字段：`homeBadge`
   - 备注：这是 hover 摇摆的元素，建议保留透明背景 PNG

3. `public/assets/home/profile-card.png`
   - 用途：左侧人物卡片
   - 当前远程字段：`homeProfileCard`
   - 备注：建议透明背景 PNG

4. `public/assets/home/phones-project.png`
   - 用途：右上手机作品拼贴
   - 当前远程字段：`homePhones`
   - 备注：当前点击进入作品详情

5. `public/assets/home/research-board.png`
   - 用途：右下木板拼贴
   - 当前远程字段：`homeResearchBoard`
   - 备注：当前点击进入作品详情

6. `public/assets/home/terminal-card.png`
   - 用途：左下终端风卡片
   - 当前远程字段：`homeTerminalCard`

7. `public/assets/qr/wechat-qr.png`
   - 用途：微信二维码弹窗内容
   - 当前状态：还没替换，仍是占位
   - 备注：建议单独准备二维码图，不要继续用贴纸图兼做二维码

8. `public/assets/common/wechat-sticker.png`
   - 用途：首页微信贴纸按钮
   - 当前远程字段：`homeWechat`
   - 备注：如果贴纸本身已经是独立切图，建议单独保留

## 2. `一些设计` 页面

建议落地素材：

1. `public/assets/design/design-hero-01.png`
   - 用途：当前页面左上主作品入口图
   - 当前远程字段：`designHero`

后续如果这个页面会继续补更多拼贴内容，建议按下面规则命名：

- `design-card-01.png`
- `design-card-02.png`
- `design-note-01.png`

## 3. 详情页公共素材

建议落地素材：

1. `public/assets/detail/detail-board-texture.webp`
   - 用途：详情页背景网格底
   - 当前远程来源：详情页背景图
   - 备注：如果和首页底板完全一致，可以直接复用 `home/board-texture.webp`

2. `public/assets/common/back-icon.png`
   - 用途：详情页左上返回图标
   - 当前远程字段：`detailBackIcon`
   - 备注：如果这是纯图标，优先导出 SVG

## 4. 作品详情内容

当前项目支持两种详情内容：长图、HTML。

来源文件参考：

- `src/content/projects.ts`

### 4.1 Keynote 导出长图

建议放到：

```text
public/project-images/
```

建议命名：

- `huikuanbao-01.png`
- `huikuanbao-02.png`
- `research-01.png`

如果单个作品很长，建议拆段导出，不要一张特别长的大图。

### 4.2 HTML 单页作品

建议放到：

```text
public/project-html/
```

建议结构：

```text
public/project-html/
  project-a/
    index.html
    assets/
  project-b/
    index.html
    assets/
```

注意：

- HTML 内部引用图片、字体、样式时，尽量都改成本地相对路径
- 不要继续依赖外部临时素材地址

## 5. 建议优先替换顺序

优先级最高：

1. 首页首屏大素材
2. `一些设计` 页面首屏素材
3. 微信二维码
4. 详情页背景和返回图标
5. 作品详情长图和 HTML 内嵌资源

原因：

- 这些资源最影响首屏加载速度
- 也是最容易受远程链接失效影响的部分

## 6. 推荐格式

通用建议：

- 透明背景拼贴：`PNG`
- 普通大图素材：`WebP`
- 特别讲究兼容时：`JPG/PNG`
- 图标：`SVG`

建议：

- 不要把所有素材都保留为原始超大尺寸
- 画面显示宽度如果最多只有 300 到 500px，没有必要保留几千像素原图

## 7. 替换位置

完成本地化后，主要改这两个文件：

1. `src/content/figmaAssets.ts`
   - 把远程 Figma 链接改成本地 `/assets/...` 路径

2. `src/content/projects.ts`
   - 把示例长图和 HTML 路径改成真实项目路径

## 8. 最小落地方案

如果你想先快速提升速度，至少先准备这 8 个本地文件：

```text
public/assets/home/board-texture.webp
public/assets/home/badge-left.png
public/assets/home/profile-card.png
public/assets/home/phones-project.png
public/assets/home/research-board.png
public/assets/home/terminal-card.png
public/assets/common/wechat-sticker.png
public/assets/qr/wechat-qr.png
```

只完成这一步，本地预览速度就会明显改善。
